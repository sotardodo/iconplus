apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicd-service-deo.fullname" . }}-test-connection"
  labels:
{{ include "cicd-service-deo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cicd-service-deo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
