# Database Configuration
DB_DRIVER=mysql-service.devops.svc.cluster.local
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=sotar
DB_PASSWORD=sotar123
DB_NAME=laravel

# Server Configuration  
# Note: Server port is currently hardcoded to 8080
# You can extend this to use SERVER_PORT environment variable if needed